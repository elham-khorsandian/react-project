import "./Card.scss" ;

const Card = props => {
    //destructuring
    const {name , tagline , first_brewed , description } = props ;

    return (
        // how can I return what I get?

    );
};

export default Card ;